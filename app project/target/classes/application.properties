# Server Configuration
server.port=8080

# Database Configuration - Using H2 for demo (easy to switch to MySQL)
spring.datasource.url=jdbc:h2:mem:healthchatbotdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for testing/demo)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.healthchatbot=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration (for frontend)
app.cors.allowedOrigins=http://localhost:8080

# MySQL Configuration (commented out - uncomment to use MySQL)
# spring.datasource.url=jdbc:mysql://localhost:3306/healthchatbot?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update

